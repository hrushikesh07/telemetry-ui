## Telemetry UI

Install dependencies:

    $ cd telemetry-ui
    $ npm install
    $ npm bower

Install gem 'sass'

    $ gem install sass
    
Run development web-server:

    $ gulp serve

## Deploy to Github pages  
  
    $ gulp build
    $ gulp deploy